#include "progargs.hpp"

#include <gtest/gtest.h>

// Ejemplo
TEST(ProgArgsTest, zeroArgsGiven) {
  // ASSERT_EQ(check_arguments(0, ), 42);  // Cambia 42 por el valor esperado real
  ASSERT_EQ(42, 42);
}
